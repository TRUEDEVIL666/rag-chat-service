meta {
  name: Upload File
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/upload-file
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

query {
  knowledge_base_id: 
}

body:multipart-form {
  file: @file()
}
