meta {
  name: Delete Users
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  [
    "user_id_1",
    "user_id_2"
  ]
}
