meta {
  name: Ask Bot (Session)
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/bots/{{bot_id}}/ask/{{session_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "message": "Hello again!",
    "streaming": false
  }
}
