meta {
  name: Ask Bot (Session)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/bots/{{bot_id}}/ask/{{session_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "message": "Tell me what you know about PowerSync",
    "streaming": true
  }
}
