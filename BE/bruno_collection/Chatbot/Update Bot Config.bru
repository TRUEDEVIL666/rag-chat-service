meta {
  name: Update Bot Config
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/bots/{{bot_id}}/config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "config_prompt": "You are a helpful assistant.",
    "config_model": {
      "model": "ollama/llama3",
      "temperature": 0.7
    },
    "kb_ids": ["<kb_id_uuid>"]
  }
}

vars:pre-request {
  bot_id: 
}
