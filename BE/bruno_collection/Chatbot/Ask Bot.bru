meta {
  name: Ask Bot
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/bots/{{bot_id}}/ask
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "message": "Hello, who are you?",
    "streaming": false
  }
}
