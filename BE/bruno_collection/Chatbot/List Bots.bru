meta {
  name: List Bots
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/bots
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.body && res.body.length > 0) {
    bru.setVar("bot_id", res.body[0].id);
  }
}
